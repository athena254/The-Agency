# Telegram beta gap and decision log

This log tracks observed gaps against [SPEC_L2_TELEGRAM_BETA.md](SPEC_L2_TELEGRAM_BETA.md). A checked CI run on a docs-only commit is **not** beta readiness. Update each entry with the exact test/revision when closed. No credentials or user message contents belong here.

| ID | State | Evidence / risk | Owner and closure proof |
|---|---|---|---|
| B-01 | Implemented, pending PR review/deploy | Draft PR #9 includes opt-in nonempty positive Telegram user-ID allowlist and private-chat preflight to both entry paths; typoed beta mode raises rather than silently disabling the gate. | 733 offline tests, Ruff and mypy passed locally on `0991f9b`; all seven CI checks passed at `ca65079`. Owner-side deny/admit probes after deployment still required. |
| B-02 | Fix committed; pending independent review/deploy | Commit `a199b18` bypasses legacy creation-intent parsing in beta and refuses detected creation attempts before Butler. Regressions cover `my name is Alice` (ordinary chat, no proposal/votes) and `Please create one called StockBot for finance` (deterministic refusal, no Butler/proposal/votes). The old bypass occurred despite earlier green CI. | Full local echo suite, Ruff, format and mypy passed in the fix worktree; review the exact PR head and CI, then test the admitted path before inviting anyone. This fix alone does not close the other beta blockers. |
| B-03 | Pushed on draft PR #10; replay unverified | PR #9 adds a persistent beta polling cursor. MVP commit `8d577dc` adds bounded backoff on handler/offset-persistence failures without advancing past the failed update; focused 23-test polling/delivery suite and consolidated offline tests passed. A crash after send but before durable commit can still duplicate a reply. | Review the exact new PR head/CI and perform one-poller replay, crash and persist-failure probes on an isolated token/runtime before inviting users. This improvement has not been deployed to the existing bot. |
| B-04 | Open | `tools/registry.py` validates schema but not authorization. `tools/builtin/memory.py` accepts caller-provided owner and can search unscoped; tool risk labels are not L0-L5 grants. | Tool-boundary slice: pre-call policy/default-deny in beta, no cross-user memory, no arbitrary write/exec. Test direct registry invocation and real SQLite isolation. |
| B-05 | Open; `web_fetch` disabled for beta | PR #9 still has unrestricted fetch behavior. A local Muse web-security slice checks each URL/redirect and DNS result, but the HTTP client does not pin the checked address: DNS can change between validation and connection and reach a private IP. Per-hop checks alone do **not** close SSRF. The slice is not uploaded to PR #9. | Bind the actual connection to a validated address (or use a verified egress proxy) while preserving HTTP Host/TLS verification; cover DNS-change and redirect attacks in transport tests and review end to end. Do not enable `web_fetch` merely because the local per-hop tests pass. |
| B-06 | Partially fixed on pushed draft PR #10; legacy end-to-end unverified | The tool driver now returns `parse_error` instead of a successful answer after repeated malformed output, Butler rejects non-completed task status, and `/research` no longer labels failed/timeout/cancelled executions complete or echoes their raw output (`ce66f64`, `23fed82`, `3eb740c`). | Focused regressions and full 844-test offline suite passed; review the exact pushed PR #10 head. Still test a real provider and the complete handler→Butler→tool failure path before inviting users. |
| B-07 | Partially implemented; model budget/rate wiring open | The beta handler rejects text above `TelegramConfig.max_message_length` before routing, and a standalone rate-budget slice exists, but the per-invitee budget is not proven in the full handler→Butler→model path. | Wire and measure deterministic per-invitee request/model ceilings (including `/research`) before any live provider call; test limit, reset and failure handling across restart. |
| B-08 | Blocked | Docker client exists but daemon was unavailable during discovery. Process backend is not hostile-code isolation. | L2 extension: enable only after actual backend, no-host-secret/FS/network, resource-limit, timeout-cleanup and synthetic-target tests on deployment host. Until then no L2 claim. |
| B-09 | Running merged main; beta rollout unverified | A single bot process is running from the clean `agency-deploy-main` checkout at merged `main` revision `4cf28a7`; startup and read-only Bot API probes were checked. No owner-confirmed live `/status`, chat or `/research` reply, rollback drill or MVP revision deployment has been verified. | Keep the running bot untouched while the MVP branch is blocked. Before beta handoff, prove one-poller startup, owner-side reply, DB backup/recovery and rollback with the intended runtime configuration. |
| B-10 | Local browser prototype only; full GUI deferred | A responsive localhost UI and isolated read-only Butler chat were implemented and smoke-tested with an offline echo provider. This is not the full GUI, authenticated multiuser service or peer-governed Agency; no consequential creation/voting is enabled through it. | Keep UI on loopback with explicit provider/privacy disclosure; exercise a real permitted provider and peer-consensus path before calling it a fully working MVP. |
| B-11 | Separate dirty checkout | `C:/Users/alphi/theagency` has uncommitted source/config/lockfile and live SQLite changes; these are not reviewed or uploaded as part of PR #8/#9. | Inspect and cherry-pick only proven intended changes in isolated PRs; do not sweep in runtime DB or weaken lint. |
| B-12 | Open; webhook beta unsupported | Default `telegram/server.py:create_app()` constructs a handler without a live Butler. With a webhook secret set, admitted users can reach `/name` but `/research` reports Butler absent. Webhook secret also does not establish a working task backend. | Transport integration must either wire and lifecycle-test a real Butler or reject beta webhook startup; exercise authenticated admitted/uninvited updates end to end. Polling-only beta must keep webhook disabled. |
| B-13 | Open; local integration held back | A Muse-authored orchestrator seam retained on branch `feat/beta-integration` (its finished worktree was removed during consolidation) passed 804 offline tests but catches beta audit-append failures and still returns success. This is fail-open relative to the durable-audit contract; it is **not** on PR #9. Muse's free endpoint then returned 429 before the regression/fix could be committed. | Add a failing audit-write injection test; in beta fail the task/reply before any success or memory claim when audit persistence fails. Re-run full offline suite, Ruff/mypy, review and only then upload. Do not enable beta tools or invite users until this closes. |
| B-14 | Open; local tool-policy work held back | Read-only review of the local policy commit found `build_default_registry(..., beta_policy=...)` silently ignores the policy keyword, allowing an unauthenticated `memory_query` to execute through that factory. It also found that `BetaPrincipal` can default to private scope or accept a non-boolean private flag. These commits are **not** on PR #9. | Reject unsupported policy kwargs or pass the policy through and test direct factory calls; require explicit strict boolean private scope in trusted-principal construction. Review a real handler→orchestrator→registry path. Do not upload/enable the policy as a security claim until fixed. |
| B-15 | Fix pushed on draft PR #10; live logs unverified | Commit `af32ce1` removes the `web_search` query from application structured logs and redacts exception bodies in returned search errors. Captured-log/error regressions pass locally. This does **not** prove that HTTP transport/debug logs cannot include request URLs, nor that DuckDuckGo returns working live search results (the probe returned HTTP 202 with no result links). | Independently review all app/transport logs with synthetic private input and verify a successful live search through the supported adapter before enabling beta research. The fix is on pushed draft PR #10, not uploaded to PR #9. |
| B-16 | Fix committed; pending review/deploy | Commit `a199b18` refuses unknown slash commands in beta before Butler/model invocation, including direct `process_message` use. | Focused admission regressions and local full suite passed; verify the PR head and keep beta blocked by remaining gates. |
| B-17 | **Architecture gap; only beta creation guarded** | The current Lattice governance stores proposals in one process, mirrors to backend best-effort, can pass on one approve and has a `user` override. It neither snapshots affected consumers nor proves multi-peer replication/convergence. Butler must remain only the human interface, not an agent/governance authority. The non-beta legacy Telegram creation route still auto-casts Butler and `user` votes; it is not safe to expose as peer consensus. | See `SPEC_PEER_LATTICE_GOVERNANCE.md`: disable legacy synthetic-vote creation across all live routes, design peer identity/affected graph/decision policy, and test three independent peers, attributable ballots, missing consumer, replay, partition/reconnect and durable failure. Do not claim the Agency is fully decentralized or turn on creation/policy mutation through UI/Telegram. |
| B-18 | **Partially closed on consolidated MVP branch; legacy path still blocked** | Review of `9053478` reproduced `memory_query` reading another sender's memory, `memory_write` despite `memory_enabled=False`, private chat mirrored to Lattice task payload, and failed adapter text returned as HTTP 200. This branch routes `POST /api/chat` only through a new isolated read-only Butler path (`ButlerService.handle_isolated_message` → `AgencyOrchestrator.converse_isolated`): no tool driver, no memory recall/write, no Lattice task mirror, sanitized audit (lengths only), and real status mapped to `200`/`502`/`504`; it fails closed with `503` when the service lacks the isolated method and never falls back to `handle_message`. Regressions in `tests/test_prototype_app.py` cover a model-requested `memory_query` (nothing dispatched), seeded victim memory, a recording tool driver, a recording Lattice, and a failing-model 502. | Still open, not claimed safe: app-level loopback peer rejection is tested but does not enforce the Uvicorn bind or authenticate the local caller; `/api/health` still reports local Butler startup, not peer consensus; the legacy `handle_message` path (Telegram) intentionally keeps tools + Lattice mirror and stays gated by B-04/B-06/B-13 and the disabled beta. Do not deploy the UI with live credentials or claim privacy until the bind is enforced and the legacy path is separately gated. |

**Rule:** fix a discovered bug in the smallest owner slice if safe and tested; otherwise assign an ID here with evidence, owner and failing/closure check. Do not promote `Open`/`Blocked`/`Unverified` to complete without reading back the exact PR head and test result.
