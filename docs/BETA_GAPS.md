# Telegram beta gap and decision log

This log tracks observed gaps against [SPEC_L2_TELEGRAM_BETA.md](SPEC_L2_TELEGRAM_BETA.md). A checked CI run on a docs-only commit is **not** beta readiness. Update each entry with the exact test/revision when closed. No credentials or user message contents belong here.

| ID | State | Evidence / risk | Owner and closure proof |
|---|---|---|---|
| B-01 | Implemented, pending PR review/deploy | Draft PR #9 includes opt-in nonempty positive Telegram user-ID allowlist and private-chat preflight to both entry paths; typoed beta mode raises rather than silently disabling the gate. | 733 offline tests, Ruff and mypy passed locally on `0991f9b`; all seven CI checks passed at `ca65079`. Owner-side deny/admit probes after deployment still required. |
| B-02 | Implemented, pending PR review/deploy | Draft PR #9 denies `/propose_agent`, `/propose-agent`, `/proposals` and detected plain-English creation before governance; polling command menu omits creation in beta. | Admission and delivery local tests; all seven CI checks passed at `ca65079`. Prove no governance state changes and verify the menu from Telegram after deployment before closing. |
| B-03 | Implemented, pending PR review/deploy | Draft PR #9 at `ca65079` adds persistent beta polling cursor, advances it after successful handling, and retries on persistence failure. A crash after send but before durable commit may still duplicate a reply. | `725 passed` in the delivery worktree with offline echo, Ruff, format, mypy; all seven CI checks passed at `ca65079`. Replay and persist-failure probes after rollout still required. |
| B-04 | Open | `tools/registry.py` validates schema but not authorization. `tools/builtin/memory.py` accepts caller-provided owner and can search unscoped; tool risk labels are not L0-L5 grants. | Tool-boundary slice: pre-call policy/default-deny in beta, no cross-user memory, no arbitrary write/exec. Test direct registry invocation and real SQLite isolation. |
| B-05 | Open | `tools/builtin/web.py` fetches arbitrary http(s) and follows redirects with no private-IP/localhost/DNS-rebinding boundary. A read-only label does not prevent SSRF. | Web-security slice: validate target and each redirect plus resolved destination, reject private/loopback/link-local/reserved addresses; mock DNS/transport tests. Keep `web_fetch` disabled in beta until verified. |
| B-06 | Open | `tools/driver.py` labels repeated malformed LLM output as completed and may return invalid text as a successful answer; `butler/service.py` returns output without rejecting failed task status. | Integrity slice: non-success statuses through driver→orchestrator→Butler→Telegram, focused regressions for parse failure, timeout and tool failure. |
| B-07 | Open | No measured per-invitee rate/model-budget cap or input-length cap before paid/live provider use. | Integration slice: deterministic length/rate ceilings with tests before LLM, `/research` included. |
| B-08 | Blocked | Docker client exists but daemon was unavailable during discovery. Process backend is not hostile-code isolation. | L2 extension: enable only after actual backend, no-host-secret/FS/network, resource-limit, timeout-cleanup and synthetic-target tests on deployment host. Until then no L2 claim. |
| B-09 | Unverified | Old Telegram poller was deliberately left running. There is no confirmed live `/status`, chat or `/research` reply from merged `main` and no verified replacement env/rollback. | Operator rollout: one-poller handoff with token source/interpreter/DB backup/rollback and owner-side message confirmation. Do not restart just to close a checklist. |
| B-10 | Deferred | Public Butler HTTP has no authenticated user scope. Full GUI, autonomous Forge, durable voting are not beta-ready. | Exclude from Telegram-first MVP; separate reviewed specs/PRs before enabling public access or high-impact actions. |
| B-11 | Separate dirty checkout | `C:/Users/alphi/theagency` has uncommitted source/config/lockfile and live SQLite changes; these are not reviewed or uploaded as part of PR #8/#9. | Inspect and cherry-pick only proven intended changes in isolated PRs; do not sweep in runtime DB or weaken lint. |
| B-12 | Open; webhook beta unsupported | Default `telegram/server.py:create_app()` constructs a handler without a live Butler. With a webhook secret set, admitted users can reach `/name` but `/research` reports Butler absent. Webhook secret also does not establish a working task backend. | Transport integration must either wire and lifecycle-test a real Butler or reject beta webhook startup; exercise authenticated admitted/uninvited updates end to end. Polling-only beta must keep webhook disabled. |
| B-13 | Open; local integration held back | A Muse-authored orchestrator seam in `C:/Users/alphi/agency-beta-integration` passed 804 offline tests but catches beta audit-append failures and still returns success. This is fail-open relative to the durable-audit contract; it is **not** on PR #9. Muse's free endpoint then returned 429 before the regression/fix could be committed. | Add a failing audit-write injection test; in beta fail the task/reply before any success or memory claim when audit persistence fails. Re-run full offline suite, Ruff/mypy, review and only then upload. Do not enable beta tools or invite users until this closes. |

**Rule:** fix a discovered bug in the smallest owner slice if safe and tested; otherwise assign an ID here with evidence, owner and failing/closure check. Do not promote `Open`/`Blocked`/`Unverified` to complete without reading back the exact PR head and test result.
