# PR #7 gate workstream A — core exception boundaries + orchestrator types

Read `docs/SPEC_PR7_READINESS.md`, `.github/workflows/ci.yml`, and actual source before editing. The branch is a draft PR; do not touch the owner's `C:/Users/alphi/theagency` or other worktrees. Work ONLY in your assigned source files: `src/agency/orchestrator.py`, `src/agency/butler/server.py`, `src/agency/butler/service.py`, `src/agency/bridges/coordinator.py`, `src/agency/agents/demo/agent.py`, `src/telegram_bot.py`, plus new focused tests or existing tests directly covering these paths (do not modify tests another worker owns). No new product features. Never use live providers or network calls in tests.

Reproduce: `C:/Users/alphi/agency-core-build/.venv/Scripts/ruff.exe check src/agency/orchestrator.py src/agency/butler/server.py src/agency/butler/service.py src/agency/bridges/coordinator.py src/agency/agents/demo/agent.py src/telegram_bot.py --output-format concise` and `C:/Users/alphi/agency-core-build/.venv/Scripts/mypy.exe src/ --no-error-summary`. Core Ruff diagnostics are BLE001 (broad catches) and S110 (`except: pass`), plus orchestrator mypy errors (`_spawned_proposals`, `_lattice`, `get_proposal_status`, `get_events`, optional tool driver, task type, generic dict). Preserve deliberately broad asynchronous/event boundary failure handling where it is needed; for each such catch, add narrow inline justification and line-local noqa only after investigating callers and regression behavior. Never globally disable lint, swallow failures silently, or turn errors into successes. Fix real type-contract bugs and add precise tests if behavior changes.

Run scoped Ruff and affected pytest with `PYTHONPATH=C:/Users/alphi/agency-gate-core/src C:/Users/alphi/agency-core-build/.venv/Scripts/python.exe -m pytest ...`; this worktree has no own venv. Do not run `uv sync` or change `uv.lock`. Commit only owned source/test paths after verifying. Report scoped Ruff result, affected mypy diagnostics, tests, commit SHA, and unresolved concerns. Do not push or merge.