# PR #7 gate workstream C — external-facing typing contracts

Read `docs/SPEC_PR7_READINESS.md`, `.github/workflows/ci.yml`, and actual implementations before editing. The draft PR is isolated from the owner's uncommitted `main`. Work ONLY in `src/agency/telegram/**`, `src/agency/llm/**`, `src/agency/bridges/**` EXCEPT `src/agency/bridges/coordinator.py`, and `src/agency/api/**`, plus directly corresponding tests. Do not edit anything else or push/merge. No live provider/API requests in tests. Preserve fail-closed explicit-provider routing and no echo fallback (`tests/test_llm_router.py`).

Run `C:/Users/alphi/agency-core-build/.venv/Scripts/mypy.exe src/ --show-error-codes --no-error-summary` in your worktree. Expected issues: httpx response json as `Any`, LLM config/adapter returns and stream annotation, Telegram handler generic dict, bridge unused ignores/return contracts, API governance return. Fix with actual validation, typed locals or precise signatures; do not globally relax strictness or add blanket ignores. Some dependent libraries lack stubs in a separate temporary 3.11 venv, but the project `.venv` and CI have 53 real diagnostics; focus on those. Add narrow tests for any behavior change. Avoid formatting-only edits.

Use `PYTHONPATH=C:/Users/alphi/agency-gate-external/src C:/Users/alphi/agency-core-build/.venv/Scripts/python.exe -m pytest <affected tests>` for tests, and the existing `.venv/Scripts/ruff.exe` for lint. This worktree has no venv; do not run `uv sync` or modify `uv.lock`. Commit only owned paths, report actual before/after diagnostics, tests, SHA, and blockers. Do not push/merge.