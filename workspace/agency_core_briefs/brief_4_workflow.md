# Muse Spark 1.3 brief — deterministic workflow v1

MIT/Python 3.11 The Agency. Read `docs/SOURCE_CONSOLIDATED_BRIEF.md` sections 2,9-10,17-18,28,40, `docs/SPEC_AGENCY_CORE_RECONCILIATION.md`, and `docs/SPEC_WORKFLOW_V1.md` FIRST. Map `src/agency/tools/registry.py`, `src/agency/kernel/policies.py`, `tests/test_tools_base.py` read-only and report what is already implemented. Other agents create skill and Butler thread files in other worktrees; do not import or depend on them. The original `main` is dirty; leave it untouched.

You may ONLY CREATE `src/agency/workflows/__init__.py`, `src/agency/workflows/registry.py`, `src/agency/workflows/executor.py`, `tests/test_workflow_registry.py`. Do not modify existing files, do not commit, do not modify uv.lock. No network, Docker, live model API, arbitrary code/shell execution.

Implement immutable dataclasses `WorkflowStep(step_id:str, operation:str, depends_on:tuple[str,...]=(), permissions:tuple[str,...]=())`, `WorkflowDefinition(workflow_id:str, version:str, steps:tuple[WorkflowStep,...])`, `WorkflowRun(run_id:str, workflow_id:str, version:str, actor_id:str, status:str, step_results:dict[str,dict], created_at:str, completed_at:str|None)`. Defensive copies where nested mutable values exist. `WorkflowRegistry(db_path:str)` opens sqlite3, initializes definitions/runs tables, has `register(definition)->WorkflowDefinition`, `get(workflow_id,version)->WorkflowDefinition`, `list_versions(workflow_id)->list[WorkflowDefinition]`, `get_run(run_id)->WorkflowRun`, `close()`. Internal `save_run` or public `record_run` used by executor. Validate identifiers, exact dotted numeric version, unique steps, known dependency, no cycles, max 64 steps, unique version; preserve insertion order. SQL parameterized. Bounded JSON input/output (64KB each). Registry no external code execution.

`WorkflowExecutor(registry:WorkflowRegistry, operations:Mapping[str, Callable], authorizer:Callable[[str,str], bool] | None=None)` and `run(workflow_id:str,version:str,inputs:dict,actor_id:str)->WorkflowRun` sync. Callbacks receive `(inputs:dict, previous_outputs:dict[str, object])` and return JSON serializable output. Determine stable topological order (declaration index tie-break), execute each step only after deps completed. Before invoking, check registered operation and each required permission using authorizer; default deny nonempty permissions, no trusting request-supplied permission list. Unknown operation or denied permission must fail closed and persist FAILED status; callback exception records error, stops subsequent steps. Registry supports reopen/restart inspection; never claims automatic resume or exactly once. Actor must be nonblank. Each run pins exact version. Avoid exposing stack traces/secrets in persisted errors.

Tests in named file: registration/restart, malformed version/duplicate/cycle/unknown dep/too many steps, deterministic ordering and output flow, old version unaffected by new version, unknown operation and permission denial do not invoke callback, failure stops later steps and persists, oversized/non-JSON rejection, SQL injection-as-data. Run `uv run --no-sync python -m pytest tests/test_workflow_registry.py -q` and `uv run --no-sync ruff check src/agency/workflows tests/test_workflow_registry.py`; report exact output, files, deviations. STOP before modifying Butler/Forge/CLI; coordinator integrates after review.