# Muse Spark 1.3 brief — Agency-native versioned skill registry

Repository: MIT/Python 3.11 The Agency. Read `docs/SOURCE_CONSOLIDATED_BRIEF.md` sections 2,7-10,17,23,27-29,45-50 and `docs/SPEC_AGENCY_CORE_RECONCILIATION.md` first. ATHENA/global-skills is separate; do not import/copy its code. Other agents concurrently create Butler thread files, do not reference them. Do not modify any existing files; ONLY CREATE `src/agency/skills/__init__.py`, `src/agency/skills/registry.py`, `tests/test_skill_registry.py`. Never touch the original dirty main worktree. Do not edit uv.lock or commit.

CHECK-1: inspect `src/agency/kernel/policies.py`, `src/agency/tools/registry.py` and report whether they already authorize/discover reusable skills. CHECK-2: inspect `tests/test_registry.py` for convention. CHECK-3: `git status --short` and report files you own. Read-only map before edits.

GOAL: provide a durable registry of reusable capability metadata, not an arbitrary script runner. Define `SkillStatus(str, Enum)` with `DRAFT, TESTING, APPROVED, PUBLISHED, DEPRECATED, RETIRED`. Define immutable dataclass `SkillSpec(skill_id:str, version:str, name:str, description:str, inputs:dict, outputs:dict, permissions:tuple[str,...], implementation:str, provenance:str, status:SkillStatus=SkillStatus.DRAFT)`; defensive copies on serialization and lookup to prevent caller mutation. `SkillRegistry(db_path: str)` creates SQLite table keyed `(skill_id,version)`, `close()`, `register(spec)->SkillSpec`, `get(skill_id,version)->SkillSpec`, `list_versions(skill_id)->list[SkillSpec]`, `publish(skill_id,version,evidence:str)->SkillSpec`. Validate nonblank identifiers/version/name/implementation/provenance; version must be a stable dotted numeric triplet `major.minor.patch` (do not normalize malformed tokens); permissions only from explicit constructor `allowed_permissions: frozenset[str]` (default empty), reject unrecognized. `implementation` must be a logical registered operation name (`[a-z][a-z0-9_.-]*`), never path/import/URL. `inputs`/`outputs` JSON serializable mappings; cap serialized size 64KB each. Use stdlib sqlite3 parameterized SQL; unique immutable versions (duplicate register raises ValueError); publish requires nonempty evidence and status APPROVED, creates a new status transition on same record rather than changing versioned definition fields; only DRAFT->TESTING->APPROVED->PUBLISHED->DEPRECATED->RETIRED transitions via `transition(skill_id,version,to_status,evidence)` with evidence required for approval and publication. Reject illegal transitions, and never provide executable code evaluation. Returns should not expose mutable internal structures. Persistence across restart. No network, live key, Docker.

Test using pytest tmp_path: restart persistence, duplicate rejection, immutability incl nested dicts, invalid token/operation/permission, illegal state transitions, publish without evidence or approval, legal lifecycle, unknown id and SQL injection-as-data. Run `uv run --no-sync python -m pytest tests/test_skill_registry.py -q` and `uv run --no-sync ruff check src/agency/skills tests/test_skill_registry.py`. Report exact files/results and deviations. STOP before wiring to Butler/agent runtime; coordinator will integrate after review.