# PR #7 gate workstream B — lattice, risk, memory typing

Read `docs/SPEC_PR7_READINESS.md`, `.github/workflows/ci.yml`, and actual source before editing. The draft PR is isolated from the owner's uncommitted `main`. Work ONLY in `src/agency/lattice/**`, `src/agency/risk/scoring.py`, `src/agency/tools/builtin/memory.py`, `src/agency/memory/sms/secrets.py` and directly corresponding tests. Do not modify other source files, config, lockfile, or GitHub. No live network or providers. Do not weaken strict mypy or add blanket ignores/casts without runtime validation.

Reproduce via `C:/Users/alphi/agency-core-build/.venv/Scripts/mypy.exe src/ --show-error-codes --no-error-summary` in your worktree and filter for owned paths. Expected issues: Lattice backend sqlite `bool` return from sqlite/Any; Lattice api imported runtime classes and fallback guards; risk scoring union return and sqlite Row handling; memory tool optional string `strip`; secret bytes/string path and decode; yaml stubs (`types-PyYAML` may be needed in the actual CI dependency environment; don't add a broad ignore). Correct signatures and contracts at the source; add regression tests for changed runtime semantics. Avoid cosmetic formatting, which will be done once after integration.

Use the existing integration venv to run targeted tests with `PYTHONPATH=C:/Users/alphi/agency-gate-data/src C:/Users/alphi/agency-core-build/.venv/Scripts/python.exe -m pytest ...`. It has type checker available at `.venv/Scripts/mypy.exe`; this new worktree has no venv. Do not run `uv sync` or edit `uv.lock`. Check owned Ruff diagnostics and commit only owned source/tests; report before/after mypy diagnostics, actual tests and commit SHA. Do not push/merge.