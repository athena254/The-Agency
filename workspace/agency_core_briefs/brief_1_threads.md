# Muse Spark 1.3 brief — persistent Butler thread foundation

Repository: MIT/Python 3.11 The Agency. Branch `architecture/agency-core` in an isolated worktree. Read `docs/SOURCE_CONSOLIDATED_BRIEF.md` sections 2,5,6,19,23,45-50 and `docs/SPEC_AGENCY_CORE_RECONCILIATION.md` first. ATHENA is separate. Do not modify any existing files; ONLY CREATE `src/agency/butler/threads.py` and `tests/test_butler_threads.py`. The original main worktree is dirty: never touch it. Other agents create skill files concurrently; do not reference them.

CHECK-1: read `src/agency/butler/service.py` (especially recall/store) and `src/agency/memory/sms/store.py`; report current sender scope and SQLite conventions before editing. CHECK-2: inspect `tests/test_butler.py`; report test conventions. CHECK-3: `git status --short`, note that only your two new paths are yours. Do not alter `uv.lock` or commit. No network, Docker, raw keys, or external service.

GOAL: Implement a persistent, owner-checked thread metadata/message store for Butler, not Butler API integration yet. Provide explicit workspace and thread boundaries. Use stdlib sqlite3 for deterministic local persistence; use parameterized queries; ensure transactions. Implement immutable dataclasses `Workspace(id, owner, title, created_at)`, `Thread(id, workspace_id, owner, title, parent_thread_id, state, created_at, updated_at)`, `ThreadMessage(id, thread_id, role, content, created_at)`. `ThreadStore(db_path: str)` initializes schema (including foreign keys, indexes), `close()`, `create_workspace(owner, title)->Workspace`, `list_workspaces(owner)->list[Workspace]`, `create_thread(owner, workspace_id, title, parent_thread_id=None)->Thread`, `get_thread(owner, thread_id)->Thread`, `list_threads(owner, workspace_id)->list[Thread]`, `append_message(owner, thread_id, role, content)->ThreadMessage`, `list_messages(owner, thread_id)->list[ThreadMessage]`. Require nonblank owner/title/content and role in `user|assistant|system`; allow only owner of workspace to create a thread; branch parent must be in same workspace and owner; never copy parent messages implicitly. Unknown/wrong-owner id returns same `KeyError` (no existence oracle). IDs uuid4 hex; timestamps ISO UTC. `:memory:` must work, temp-file DB must persist across restart; close idempotent. Fail closed for invalid inputs. Do not store secrets or infer identity from model text.

Tests in your one new test file: restart persists workspace/thread/messages; two threads of one owner do not cross-contaminate; other owner cannot get/append/list/branch; parent across workspace denied; no implicit parent message inheritance; input validation; SQL injection strings treated as data. Use pytest tmp_path. Run `uv run --no-sync python -m pytest tests/test_butler_threads.py -q` and `uv run --no-sync ruff check src/agency/butler/threads.py tests/test_butler_threads.py`. Report exact files, test counts, any deviations. STOP before changing ButlerService, server, or other files — coordinator will wire it in after independent foundation review.